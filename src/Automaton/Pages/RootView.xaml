<Window
    x:Class="Automaton.Pages.RootView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Automaton.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:s="https://github.com/canton7/Stylet"
    xmlns:usercontrols="clr-namespace:Automaton.UserControls"
    Title="Automaton"
    Width="800"
    Height="450"
    Background="{DynamicResource BackgroundColor}"
    mc:Ignorable="d">
    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <!--<usercontrols:BrowseModpackView s:View.Model="{Binding BrowseModpack}" />-->
        <!--<Label
            HorizontalAlignment="Center"
            Content="{Binding ModCount}"
            FontSize="15"
            Foreground="White" />-->

        <Label
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Content="{Binding DebugOut}"
            FontSize="16"
            Foreground="White" />

    </Grid>
</Window>
